// Global shared types

export interface AuthorProfile {
  id: string;
  display_name: string | null;
  avatar_url: string | null;
}

// Outros tipos globais podem ser migrados para cá conforme necessário.
